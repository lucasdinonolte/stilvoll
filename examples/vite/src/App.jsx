import { u } from 'stilvoll';

function App() {
  return (
    <div className={u}>
      <div>OMG, typesafe utility classes, so nice.</div>
      <div>OMG, typesafe utility classes, so nice.</div>
    </div>
  );
}

export default App;
