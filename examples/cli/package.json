{
  "name": "cli-playground",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p ./dist && stilvoll --watch",
    "build": "mkdir -p ./dist && stilvoll --verbose"
  },
  "dependencies": {
    "stilvoll": "*",
    "@stilvoll/cli": "0.0.4",
    "@stilvoll/rules": "0.0.4"
  }
}
